MODE=target-only
CONTAINER_NAME=dsv4-target-only-f8ea5bb
SERVED_MODEL_NAME=deepseek-v4-flash-0731-target

# Field-tested production defaults. The canonical production launcher also
# pins these values explicitly so an interactive shell cannot inherit an old
# tuning experiment by accident.
MODE_GPU_MEMORY_UTILIZATION=0.92
MODE_MAX_NUM_BATCHED_TOKENS=4096
