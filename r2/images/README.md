# Offline image payload

The source repository intentionally does not contain Docker image archives.
`scripts/package_offline_release.sh` creates these files in the final delivery:

- `dsv4-a100-r2-image.tar`
- `dsv4-a100-r2-image.tar.sha256`
- `offline-image.env`
