# Runtime results

Benchmark, stability, API-contract, image-inspect, and GPU evidence files are
written below `RUNTIME_ROOT` at runtime and intentionally excluded from source
control. `r1/scripts/collect_results.sh` places the final secret-free compressed
evidence bundle and its SHA-256 file in this directory.
