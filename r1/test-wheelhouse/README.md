# Offline test wheelhouse

The complete offline delivery contains the wheels listed in `sha256sums.txt`.
They are intentionally omitted from the public source-only GitHub repository.
The source-test image build must use the exact files whose hashes are recorded
here; do not substitute an online install on the target machine.
